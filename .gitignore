# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Intermediate/duplicate data
data/combined_results.jsonl
data/pilot/
data/processed/
data/items/

# Intermediate results (regenerable from data + scripts)
results/all/
results/combined/
results/figures/
results/gpt52/
results/gpt52-interim/
results/interim/
results/openai/
results/pilot/
results/tables/

# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.synctex*
paper/*.fls
paper/*.fdb_latexmk

# API keys should never be committed
.env
*.env
data/raw/experiment_resume.log
data/annotators/_identities.md
data/annotators/_excluded/
client_secret*.json
*.json.credentials
results-*.json
data/annotators/_raw/

# Claude Code / local tooling state
.claude/

# Internal planning notes (do not publish review-process drafts)
plans/

# Pre-publication backup snapshots (kept for personal recovery only)
data/all_results.pre_*_backup.jsonl
results/*.pre_*_backup.json
# NB: results/paper_statistics_v{1,2}.json are intentionally NOT ignored —
# they are consumed by analysis/paper_statistics_adjudicated.py.

# arXiv tarball is a build artifact (regenerate via scripts/build_arxiv_tarball.sh)
amel-arxiv.tar.gz
